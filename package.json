{
  "name": "photography-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Vanilla single-page photography site with Tailwind CSS (local build)",
  "scripts": {
    "dev": "tailwindcss -i src/styles/input.css -o public/styles.css --watch",
    "build": "tailwindcss -i src/styles/input.css -o public/styles.css --minify && cp -R data public/"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.47",
    "tailwindcss": "^3.4.14"
  }
}


